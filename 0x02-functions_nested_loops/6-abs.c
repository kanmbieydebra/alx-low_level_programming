#include "main.h"

/**
  * _abs - returns absolute value
  *@n: interger value
  * Return: 1 if character is lowercase, 0 otherwise.
  */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
